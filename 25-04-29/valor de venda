# fazer um programa para solicitar o valor da venda
#e o percentual da comissao e exibir o valor da comissao

valor_venda = float(input('Digite o valor da venda(R$):'))
percentual = float(input('Digite o valor da comissao (%):'))
comissao = valor_venda * percentual/100
print(f'o valor da comissao é R${comissao:.2f}')
